In the project directory, you can run:

#### Run dev-server in browser

#### `yarn start --web`

#### Run development electron app

#### `yarn electron-dev`

#### Build application

#### `yarn electron-pack`
